{
  "type": "module",
  "devDependencies": {
    "@types/node": "^22.7.5",
    "gts": "^6.0.2",
    "nodemon": "^3.1.9",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
